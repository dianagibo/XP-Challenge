const service = require('./bonus.service');
function flash(request) { const value = request.session.flash; delete request.session.flash; return value; }
async function showList(request, response, next) { try { const data = await service.list(request.session.user); response.render('pages/bonuses', { pageTitle: 'Reconocimientos especiales', activePage: 'bonuses', ...data, flash: flash(request) }); } catch (error) { next(error); } }
async function showDetail(request, response, next) { try { const bonus = await service.getById(request.params.id, request.session.user); response.render('pages/bonus-details', { pageTitle: bonus.title, activePage: 'bonuses', bonus, flash: flash(request) }); } catch (error) { next(error); } }
async function create(request, response, next) { try { const bonus = await service.create(request.body, request.session.user); request.session.flash = { type: 'success', message: '¡Reconocimiento entregado!' }; response.redirect(`/bonuses/${bonus._id}`); } catch (error) { if (error.status === 400) { request.session.flash = { type: 'error', message: error.message }; return response.redirect('/bonuses'); } next(error); } }
async function update(request, response, next) { try { await service.updateText(request.params.id, request.body, request.session.user); request.session.flash = { type: 'success', message: 'Texto actualizado.' }; response.redirect(`/bonuses/${request.params.id}`); } catch (error) { if (error.status === 400) { request.session.flash = { type: 'error', message: error.message }; return response.redirect(`/bonuses/${request.params.id}`); } next(error); } }
async function voidBonus(request, response, next) { try { await service.voidBonus(request.params.id, request.body, request.session.user); request.session.flash = { type: 'success', message: 'Reconocimiento anulado con trazabilidad.' }; response.redirect(`/bonuses/${request.params.id}`); } catch (error) { if (error.status === 400) { request.session.flash = { type: 'error', message: error.message }; return response.redirect(`/bonuses/${request.params.id}`); } next(error); } }
module.exports = { showList, showDetail, create, update, voidBonus };
